# Инструкция по безопасному деплою Traffic Light Server

## ✅ Устранённые уязвимости

1. ✅ **Пароли удалены из конфигурации** - используются переменные окружения
2. ✅ **Валидация входных данных** - проверка URL, имени пользователя, пароля
3. ✅ **Безопасное логирование** - пароли не выводятся в логи
4. ✅ **Защита файлов** - права доступа 600 для конфигурации
5. ✅ **Принцип наименьших привилегий** - сервер работает от непривилегированного пользователя

## 🚀 Запуск на сервере (Production)

### Вариант 1: Автоматическая установка (Рекомендуется)

```bash
# 1. Соберите проект
mvn clean package

# 2. Скопируйте на сервер
tar -czf svetoofor.tar.gz target/svetoofor-server.jar server.properties deploy/
scp svetoofor.tar.gz user@your-server:/tmp/

# 3. На сервере запустите установку
ssh user@your-server
cd /tmp
tar -xzf svetoofor.tar.gz
cd deploy
sudo ./install.sh

# 4. Настройте учетные данные JIRA
sudo nano /etc/systemd/system/svetoofor.service
# Измените JIRA_USERNAME и JIRA_PASSWORD

# 5. Запустите сервис
sudo systemctl daemon-reload
sudo systemctl enable svetoofor
sudo systemctl start svetoofor

# 6. Проверьте статус
sudo systemctl status svetoofor
sudo journalctl -u svetoofor -f
```

### Вариант 2: Запуск вручную с переменными окружения

```bash
# 1. Установите переменные окружения
export JIRA_URL="https://jiraportal.cbk.kg"
export JIRA_USERNAME="incident"
export JIRA_PASSWORD="qweasd123#$"

# 2. Запустите сервер
java -jar target/svetoofor-server.jar
```

### Вариант 3: Использование .env файла

```bash
# 1. Создайте .env файл
cp deploy/env.example .env

# 2. Отредактируйте .env
nano .env

# 3. Загрузите переменные и запустите
source .env
java -jar target/svetoofor-server.jar
```

## 🔐 Безопасность

### Важные файлы и их права доступа

```bash
# Конфигурация (только чтение для владельца)
chmod 600 /opt/svetoofor/server.properties
chown svetoofor:svetoofor /opt/svetoofor/server.properties

# JAR файл (чтение и выполнение)
chmod 644 /opt/svetoofor/svetoofor-server.jar
chown svetoofor:svetoofor /opt/svetoofor/svetoofor-server.jar

# НИКОГДА не храните .env файлы в git!
echo ".env" >> .gitignore
```

### Что НЕ нужно коммитить в git

Файл `.gitignore` уже настроен для исключения:
- `.env` - файлы с переменными окружения
- `server.properties.local` - локальные конфигурации
- `*.log` - логи
- `target/` - скомпилированные файлы

### Рекомендации по паролям

1. **Не используйте простые пароли** типа "password" или "123456"
2. **Используйте уникальные пароли** для каждого окружения (dev, staging, prod)
3. **Регулярно меняйте пароли** (рекомендуется каждые 90 дней)
4. **Храните пароли в системах управления секретами** (HashiCorp Vault, AWS Secrets Manager и т.д.)

## 📁 Структура проекта после деплоя

```
/opt/svetoofor/
├── svetoofor-server.jar    # Приложение
└── server.properties        # Конфигурация (без паролей!)

/etc/systemd/system/
└── svetoofor.service        # Systemd сервис (с переменными окружения)

/var/log/journal/
└── svetoofor logs           # Логи (через journalctl)
```

## 🔧 Управление сервисом

```bash
# Запуск
sudo systemctl start svetoofor

# Остановка
sudo systemctl stop svetoofor

# Перезапуск
sudo systemctl restart svetoofor

# Статус
sudo systemctl status svetoofor

# Логи
sudo journalctl -u svetoofor -f           # В реальном времени
sudo journalctl -u svetoofor -n 100       # Последние 100 строк
sudo journalctl -u svetoofor --since "1 hour ago"
```

## 🧪 Тестирование

### Локальное тестирование

```bash
# С переменными окружения
export JIRA_URL="https://jiraportal.cbk.kg"
export JIRA_USERNAME="incident"
export JIRA_PASSWORD="qweasd123#$"

java -jar target/svetoofor-server.jar
```

### Проверка на сервере

```bash
# 1. Проверка что сервис запущен
sudo systemctl status svetoofor

# 2. Проверка что порт открыт
netstat -tulpn | grep 52521

# 3. Проверка подключения к JIRA
curl -u incident:qweasd123#$ https://jiraportal.cbk.kg/rest/api/2/myself

# 4. Проверка логов
sudo journalctl -u svetoofor -n 50
```

## 🔄 Обновление на сервере

```bash
# 1. Остановите сервис
sudo systemctl stop svetoofor

# 2. Замените JAR файл
sudo cp new-svetoofor-server.jar /opt/svetoofor/svetoofor-server.jar

# 3. Запустите сервис
sudo systemctl start svetoofor

# 4. Проверьте логи
sudo journalctl -u svetoofor -f
```

## ❌ Решение проблем

### Ошибка: "Connect timed out"

**Проблема**: Нет доступа к JIRA серверу

**Решение**:
1. Убедитесь что сервер находится в корпоративной сети или подключён к VPN
2. Проверьте доступность JIRA: `curl -v https://jiraportal.cbk.kg`
3. Проверьте firewall правила

### Ошибка: "JIRA URL cannot be null or empty"

**Проблема**: Переменные окружения не установлены

**Решение**:
```bash
# Проверьте переменные окружения
echo $JIRA_URL
echo $JIRA_USERNAME

# Если пусто - установите их
export JIRA_URL="https://jiraportal.cbk.kg"
export JIRA_USERNAME="incident"
export JIRA_PASSWORD="qweasd123#$"
```

### Ошибка: "Address already in use"

**Проблема**: Порт 52521 уже используется

**Решение**:
```bash
# Найдите процесс
lsof -i :52521

# Остановите его
kill -9 <PID>

# Или измените порт в server.properties
server.port=8888
```

## 📊 Мониторинг

### Создание health-check скрипта

```bash
cat > /usr/local/bin/check-svetoofor.sh << 'EOF'
#!/bin/bash
if systemctl is-active --quiet svetoofor; then
    echo "OK: Svetoofor is running"
    exit 0
else
    echo "CRITICAL: Svetoofor is not running"
    exit 2
fi
EOF

chmod +x /usr/local/bin/check-svetoofor.sh
```

### Интеграция с мониторингом

Добавьте в ваш monitoring tool (Nagios, Zabbix, Prometheus и т.д.):
```bash
/usr/local/bin/check-svetoofor.sh
```

## 📞 Поддержка

- **Email**: support@incuat.kg
- **Документация**: См. `deploy/README.md` для детальной информации
- **Логи**: `sudo journalctl -u svetoofor -f`

## ⚠️ Важные напоминания

1. ✅ **ВСЕГДА** используйте переменные окружения для паролей
2. ✅ **НИКОГДА** не коммитьте файлы с паролями в git
3. ✅ Запускайте сервис от **непривилегированного пользователя**
4. ✅ Используйте **файрвол** для ограничения доступа
5. ✅ Регулярно **обновляйте зависимости** и проверяйте на уязвимости
6. ✅ Настройте **автоматические бэкапы** конфигурации
7. ✅ Используйте **SSL/TLS** через nginx reverse proxy для production

Удачного деплоя! 🚀
