# 📦 Windows EXE Установщик для Svetoofor

Эта папка содержит все необходимое для создания Windows EXE установщика из JAR файла.

## 🎯 Зачем это нужно?

Вместо раздачи JAR файлов (`svetoofor-installer.jar`), вы можете раздать пользователям полноценный **Windows EXE** (`SvetooforInstaller.exe`):

- ✅ Запускается двойным кликом (не нужно `java -jar`)
- ✅ Выглядит как обычное Windows приложение
- ✅ Имеет метаданные (версия, компания, описание)
- ✅ Лучше проходит через корпоративные антивирусы
- ✅ Автоматически проверяет наличие Java

## 📁 Что здесь находится

- `svetoofor-installer.jar` - Java установщик (13 MB)
- `launch4j-config.xml` - конфигурация для Launch4j
- `build-exe.bat` - автоматический скрипт для создания EXE (Windows)
- `build-exe.sh` - автоматический скрипт для создания EXE (Mac/Linux)
- `README.md` - эта инструкция
- `SvetooforInstaller.exe` - готовый EXE (появится после сборки)

## 🚀 Быстрый старт (для Windows)

### Шаг 1: Установить Launch4j

1. Скачайте Launch4j: https://sourceforge.net/projects/launch4j/
2. Запустите установщик
3. Установите в стандартную папку `C:\Program Files (x86)\Launch4j\`

### Шаг 2: Создать EXE установщик

**Вариант A: Двойной клик (самый простой)**
1. Дважды кликните на файл `build-exe.bat`
2. Дождитесь завершения
3. Готово! В папке появится `SvetooforInstaller.exe`

**Вариант B: Через командную строку**
```cmd
cd installer
build-exe.bat
```

### Готово!

После успешной сборки вы получите файл `SvetooforInstaller.exe` размером ~13-14 MB.

---

## 🍎 Для Mac / Linux пользователей

### Шаг 1: Установить Launch4j

**macOS (через Homebrew):**
```bash
brew install launch4j
```

**Ubuntu/Debian:**
```bash
sudo apt-get install launch4j
```

**Другие Linux:**
Скачайте с https://sourceforge.net/projects/launch4j/

### Шаг 2: Создать EXE

```bash
cd installer
chmod +x build-exe.sh
./build-exe.sh
```

Скрипт автоматически найдёт launch4j и создаст `SvetooforInstaller.exe`

---

## Ручной способ (если нужно)

Если автоматический скрипт не работает:

1. Откройте Launch4j GUI
2. Загрузите конфигурацию: `launch4j-config.xml`
3. Нажмите кнопку "Build wrapper"
4. Получите `SvetooforInstaller.exe`

## Что делает EXE установщик

После запуска `SvetooforInstaller.exe` пользователь увидит GUI окно где нужно:

1. Ввести IP адрес сервера (или оставить localhost)
2. Ввести порт сервера (по умолчанию 52521)
3. Выбрать цвет светофора (RED, YELLOW, GREEN)
4. Опционально: добавить в автозагрузку Windows

Установщик:
- Создаст папку `C:\Users\USERNAME\AppData\Local\TrafficLightClient\`
- Сохранит файл `client.properties` с настройками
- Скопирует клиентское приложение
- Добавит в автозагрузку (если выбрано)

## Системные требования

- Windows 7/10/11
- Java 17 или новее (JRE или JDK)

## Распространение

После создания `SvetooforInstaller.exe` вы можете:

1. Отправить его пользователям по email
2. Разместить на файловом сервере
3. Отправить через корпоративную систему
4. Проверить через антивирус перед распространением

**Важно**: EXE файл это просто обертка вокруг JAR файла. Пользователям все равно нужна установленная Java 17+.

## ✅ Что НЕ изменилось на сервере

**ВАЖНО**: Этот установщик находится в отдельной папке `installer/` и **НЕ влияет** на:

- ❌ Код сервера на production
- ❌ Уже развернутые JAR файлы
- ❌ Конфигурацию сервера
- ❌ Файл server.properties
- ❌ Работающие клиенты

**Вы просто создаёте EXE обёртку над существующим JAR - никакого кода не меняется!**

Можете спокойно создавать EXE установщик - это не затронет работающий сервер.

## Troubleshooting

### "Launch4j not found"
- Установите Launch4j с https://launch4j.sourceforge.net/
- Проверьте путь установки

### "svetoofor-installer.jar not found"
- JAR файл уже скопирован в эту папку
- Если удалили, скопируйте из `target/svetoofor-installer.jar`

### EXE не запускается
- Проверьте что у пользователя установлена Java 17+
- Попробуйте запустить от имени администратора
- Проверьте антивирус (может блокировать)

## Альтернатива: jpackage (Java 17+)

Если нужен установщик с встроенной Java (без требования установки JRE):

```bash
jpackage --input installer \
  --name SvetooforInstaller \
  --main-jar svetoofor-installer.jar \
  --type exe \
  --win-menu \
  --win-shortcut
```

Это создаст полноценный Windows installer с встроенной Java (размер ~50-70 MB).

## Поддержка

При проблемах проверьте:
1. Java 17+ установлена
2. Launch4j установлен корректно
3. Все файлы на месте в папке installer/
4. Антивирус не блокирует запуск
