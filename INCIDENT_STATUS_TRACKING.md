# Отслеживание статусов инцидентов

## Что было исправлено

### 1. Проблема с аутентификацией (admin.login=test, admin.password=test)

**Проблема:** Файл `client.properties` был в неправильной кодировке (UTF-16 с BOM), что приводило к ошибкам при чтении настроек.

**Решение:** Файл пересоздан в правильной кодировке UTF-8.

Теперь логин `test` и пароль `test` будут работать корректно.

---

### 2. Отслеживание статусов инцидентов (открытие/закрытие)

**Проблема:** Система не различала:
- Создание нового инцидента
- Обновление статуса инцидента (например, переход в статус "На анализе")
- Закрытие инцидента

Все эти события вызывали красный светофор.

**Решение:** Добавлен интеллектуальный механизм отслеживания статусов в `JiraPoller.java`:

#### Новое поведение:

1. **При создании инцидента:**
   - Система запоминает инцидент как "активный"
   - Отправляет сигнал `RED_BLINK` (красный мигающий светофор на 120 сек)
   - В консоли: `📋 Новый инцидент: ITSMJIRA-123456`

2. **При обновлении статуса (например, "На анализе"):**
   - Система обновляет статус в памяти
   - **НЕ отправляет** повторный сигнал светофора
   - В консоли: `🔄 Обновление статуса инцидента: ITSMJIRA-123456`
   - Показывает изменение: `Статус: Открыт → На анализе`

3. **При закрытии инцидента:**
   - Система определяет, что инцидент закрыт
   - Удаляет инцидент из списка активных
   - Отправляет сигнал `GREEN_BLINK` (зеленый мигающий светофор на 120 сек)
   - В консоли: `✅ Инцидент закрыт: ITSMJIRA-123456`
   - Показывает изменение: `Статус: На анализе → Закрыт`

---

## Закрытые статусы

Система определяет следующие статусы как "закрытые":
- Закрыт / Closed
- Решен / Resolved
- Завершен / Done

Можно расширить список в методе `isClosedStatus()` в файле `JiraPoller.java:176`.

---

## Как это работает

### Внутренняя логика:

1. **Map activeIncidents** - хранит активные (открытые) инциденты с их текущими статусами
2. **Set processedIssues** - хранит все инциденты, которые когда-либо были обработаны

### Workflow:

```
Новый инцидент → RED_BLINK → Запомнить в activeIncidents

Обновление статуса → Обновить статус в activeIncidents → Без сигнала

Закрытие → Удалить из activeIncidents → GREEN_BLINK
```

---

## Сигналы светофора

| Событие                    | Сигнал       | Длительность | Цвет           |
|----------------------------|--------------|--------------|----------------|
| Новый инцидент             | RED_BLINK    | 120 сек      | Красный        |
| Обновление статуса         | -            | -            | Без изменений  |
| Закрытие инцидента         | GREEN_BLINK  | 120 сек      | Зеленый        |
| Очередь (управление админа)| QUEUE_GREEN  | До снятия    | Зеленый        |
| Очередь большая (админ)    | QUEUE_RED    | До снятия    | Красный        |

---

## Пример работы

### Сценарий 1: Новый инцидент
```
🔍 Опрос JIRA на наличие новых инцидентов...
   Найдено инцидентов: 1
📋 Новый инцидент: ITSMJIRA-123456
   Инцидент: Сбой сервера базы данных | Приоритет: высокий | Статус: Открыт | Автор: Иван Иванов
   🔴 Отправка сигнала: RED_BLINK (инцидент)
```

### Сценарий 2: Обновление статуса
```
🔍 Опрос JIRA на наличие новых инцидентов...
   Найдено инцидентов: 1
🔄 Обновление статуса инцидента: ITSMJIRA-123456
   Статус: Открыт → На анализе
```

### Сценарий 3: Закрытие инцидента
```
🔍 Опрос JIRA на наличие новых инцидентов...
   Найдено инцидентов: 1
✅ Инцидент закрыт: ITSMJIRA-123456
   Статус: На анализе → Закрыт
   🟢 Отправка сигнала: GREEN_BLINK (разрешение инцидента)
```

---

## Настройка

Все работает "из коробки". Никаких дополнительных настроек не требуется.

Если нужно изменить список закрытых статусов, отредактируйте метод:
```java
// src/main/java/incuat/kg/svetoofor/jira/JiraPoller.java:176
private boolean isClosedStatus(String status) {
    // Добавьте свои статусы здесь
}
```

---

## Тестирование

1. Запустите сервер:
   ```bash
   java -jar target/svetoofor-server.jar
   ```

2. Запустите клиент:
   ```bash
   java -jar target/svetoofor-client.jar
   ```

   Введите логин: `test`, пароль: `test`

3. Создайте тестовый инцидент в JIRA → Должен загореться красный светофор

4. Обновите статус инцидента на "На анализе" → Светофор не должен меняться

5. Закройте инцидент → Должен загореться зеленый светофор

---

## Файлы, которые были изменены

- `client.properties` - исправлена кодировка
- `src/main/java/incuat/kg/svetoofor/jira/JiraPoller.java` - добавлено отслеживание статусов

Проект пересобран: `mvn clean package`
