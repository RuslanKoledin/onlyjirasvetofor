# Поддержка алертов и кастомных JQL запросов

## Что было добавлено

Система теперь поддерживает:
1. **Алерты (тип 13802)** - желтый светофор
2. **Инциденты (тип 11206)** - красный светофор
3. **Кастомные JQL запросы** через конфигурацию
4. **Автоматическую фильтрацию закрытых статусов** в JQL

---

## Конфигурация в server.properties

Вы добавили в `server.properties`:

```properties
jira.url=https://jiraportal.cbk.kg
jira.username=incident
jira.password=qweasd123#$
jira.project=ITSMJIRA

# Кастомный JQL запрос (новое!)
jira.jql=project = ITSMJIRA AND issuetype IN (11206, 13802) AND status NOT IN (Closed,Resolved,Done)

jira.poll.interval=5
```

### Как это работает:

JQL запрос **уже исключает** закрытые статусы:
```
status NOT IN (Closed,Resolved,Done)
```

Это значит, что система будет получать только **открытые** инциденты и алерты.

---

## Новое поведение

### При создании:

**Инцидент (тип 11206):**
```
📋 Новая запись: ITSMJIRA-123456 (тип: Incident, ID: 11206)
   🔴 Отправка сигнала: RED_BLINK (инцидент)
```
→ Красный светофор на 120 секунд

**Алерт (тип 13802):**
```
📋 Новая запись: ITSMJIRA-789012 (тип: Alert, ID: 13802)
   🟡 Отправка сигнала: YELLOW_BLINK (алерт)
```
→ Желтый светофор на 120 секунд

### При обновлении статуса:

```
🔄 Обновление статуса инцидента: ITSMJIRA-123456
   Статус: Открыт → На анализе
```
→ Без сигнала (только логирование)

### При закрытии:

Так как JQL уже фильтрует закрытые статусы, **закрытые задачи не попадают в выборку**.

Однако система все равно отслеживает переход из открытого в закрытое состояние:

```
✅ Инцидент закрыт: ITSMJIRA-123456
   Статус: На анализе → Закрыт
   🟢 Отправка сигнала: GREEN_BLINK (разрешение)
```
→ Зеленый светофор на 120 секунд

---

## Сценарии работы

### Сценарий 1: Новый инцидент

1. Создается инцидент типа 11206 в JIRA
2. Система получает его через JQL (статус != Closed)
3. **Красный светофор 🔴** на 120 секунд
4. Инцидент запоминается как "активный"

### Сценарий 2: Новый алерт

1. Создается алерт типа 13802 в JIRA
2. Система получает его через JQL (статус != Closed)
3. **Желтый светофор 🟡** на 120 секунд
4. Алерт запоминается как "активный"

### Сценарий 3: Переход в статус "На анализе"

1. Инцидент переводится в статус "На анализе"
2. Система получает обновление (статус != Closed)
3. **Без сигнала** - только логирование изменения статуса
4. Инцидент остается в списке "активных"

### Сценарий 4: Закрытие

**Вариант A: Система успела получить обновление до закрытия**
1. Инцидент переводится в статус "Закрыт"
2. Система получает ПОСЛЕДНЕЕ обновление (статус = Closed)
3. Определяет, что статус закрыт
4. **Зеленый светофор 🟢** на 120 секунд
5. Удаляет из списка "активных"

**Вариант B: Инцидент был закрыт до опроса**
1. Инцидент уже закрыт в JIRA
2. JQL фильтрует его (status NOT IN Closed)
3. Система не получает этот инцидент
4. **Без сигнала**

---

## Типы задач

| Тип ID | Название   | Сигнал светофора | Цвет    | Длительность |
|--------|-----------|------------------|---------|--------------|
| 11206  | Incident  | RED_BLINK        | Красный | 120 сек      |
| 13802  | Alert     | YELLOW_BLINK     | Желтый  | 120 сек      |
| другой | Unknown   | RED_BLINK        | Красный | 120 сек      |

При закрытии любого типа → GREEN_BLINK (зеленый, 120 сек)

---

## Статусы

### Закрытые статусы (определяются автоматически):

Система распознает следующие статусы как "закрытые":
- Закрыт / Closed
- Решен / Resolved
- Завершен / Done

Кроме того, ваш JQL уже фильтрует их:
```
status NOT IN (Closed,Resolved,Done)
```

### Открытые статусы:

Все остальные (Открыт, На анализе, В работе и т.д.)

---

## Обратная совместимость

Если в `server.properties` **НЕТ** параметра `jira.jql`, система автоматически создаст запрос:

```jql
issuetype = 11206 AND status NOT IN (Closed,Resolved,Done)
```

Это обеспечивает работу со старыми конфигурациями.

---

## Пример работы

### Консоль сервера при запуске:

```
=== Traffic Light Server (JIRA Only) ===
Loaded server.properties
WebSocket server started on port 52521
Starting JIRA integration...
JIRA URL: https://jiraportal.cbk.kg
JIRA Username: incident
JQL Query: project = ITSMJIRA AND issuetype IN (11206, 13802) AND status NOT IN (Closed,Resolved,Done)
Poll interval: 5 minutes
Запуск JIRA Poller...
Интервал опроса: 5 минут
JQL запрос: project = ITSMJIRA AND issuetype IN (11206, 13802) AND status NOT IN (Closed,Resolved,Done)
✅ Подключение к JIRA успешно
JIRA integration started successfully
Server is running. Press Ctrl+C to stop.
```

### При обнаружении инцидента:

```
🔍 Опрос JIRA на наличие новых инцидентов/алертов...
   Найдено записей: 2
📋 Новая запись: ITSMJIRA-123456 (тип: Incident, ID: 11206)
   Инцидент: Сбой сервера БД | Приоритет: высокий | Статус: Открыт | Автор: Иван Иванов
   🔴 Отправка сигнала: RED_BLINK (инцидент)
📋 Новая запись: ITSMJIRA-789012 (тип: Alert, ID: 13802)
   Инцидент: Высокая загрузка CPU | Приоритет: средний | Статус: Новый | Автор: Мария Петрова
   🟡 Отправка сигнала: YELLOW_BLINK (алерт)
✅ Обработано новых записей: 2
```

---

## Измененные файлы

1. `server.properties` - добавлен параметр `jira.jql`
2. `client.properties` - исправлена кодировка, обновлены логин/пароль
3. `src/main/java/incuat/kg/svetoofor/jira/JiraClient.java` - добавлен метод `searchByCustomJql()`
4. `src/main/java/incuat/kg/svetoofor/jira/JiraIssue.java` - добавлено поле `issuetype`
5. `src/main/java/incuat/kg/svetoofor/jira/JiraPoller.java` - добавлено различение инцидентов и алертов
6. `src/main/java/incuat/kg/svetoofor/ServerLauncher.java` - добавлена поддержка кастомного JQL

---

## Тестирование

### 1. Запустите сервер:
```bash
java -jar target/svetoofor-server.jar
```

### 2. Запустите клиент:
```bash
java -jar target/svetoofor-client.jar
```

### 3. Проверьте работу:

**Инцидент:**
1. Создайте инцидент (тип 11206) в JIRA
2. Должен загореться **красный** светофор

**Алерт:**
1. Создайте алерт (тип 13802) в JIRA
2. Должен загореться **желтый** светофор

**Обновление статуса:**
1. Переведите инцидент в статус "На анализе"
2. Светофор **не должен меняться**

**Закрытие:**
1. Закройте инцидент
2. Должен загореться **зеленый** светофор

---

## Настройка дополнительных типов

Если нужно добавить другие типы задач, отредактируйте:

1. **server.properties** - добавьте ID типа в JQL:
```properties
jira.jql=project = ITSMJIRA AND issuetype IN (11206, 13802, 99999) AND status NOT IN (Closed,Resolved,Done)
```

2. **JiraPoller.java:162-163** - добавьте логику определения:
```java
boolean isIncident = "11206".equals(issueTypeId);
boolean isAlert = "13802".equals(issueTypeId);
boolean isCustomType = "99999".equals(issueTypeId);  // добавьте свой тип
```

3. **JiraPoller.java:172-183** - добавьте сигнал:
```java
if (isCustomType) {
    System.out.println("   🟣 Отправка сигнала: PURPLE_BLINK (кастомный тип)");
    trafficLightServer.broadcast("PURPLE_BLINK");
}
```

---

## Готово!

Теперь система корректно работает с:
- ✅ Инцидентами (красный)
- ✅ Алертами (желтый)
- ✅ Отслеживанием статусов
- ✅ Автоматическим определением типа
- ✅ Кастомными JQL запросами
