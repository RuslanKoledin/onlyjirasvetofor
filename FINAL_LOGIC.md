# Финальная логика оповещений

## ✅ Реализованная логика

### 🔴 КРАСНЫЙ светофор (инциденты, тип 11206)

**Показывается ОДИН РАЗ** когда инцидент становится активным:

**Активные статусы:**
- Создан
- Назначен
- Исполнитель
- Руководитель
- Created
- Assigned
- In Progress
- В работе

**Поведение:**
1. Инцидент создается в статусе "Создан" → 🔴 RED_BLINK (120 сек) - **ОДИН РАЗ**
2. Инцидент переходит "Создан" → "Назначен" → **БЕЗ повторного сигнала** (только логирование)
3. Инцидент переходит "Назначен" → "Исполнитель" → **БЕЗ повторного сигнала**
4. Инцидент переходит "Исполнитель" → "Руководитель" → **БЕЗ повторного сигнала**

**Красный светофор горит 120 секунд и гаснет. Повторно НЕ загорается при переходах между активными статусами.**

---

### 🟢 ЗЕЛЕНЫЙ светофор (решение)

**Показывается ОДИН РАЗ** когда инцидент переходит в решенный статус:

**Решенные статусы:**
- В ожидании
- Решен
- Закрыто
- Отклонен
- Pending
- Resolved
- Closed
- Rejected
- Done

**Поведение:**
1. Инцидент переходит из активного статуса → "Решен" → 🟢 GREEN_BLINK (**30 секунд**) - **ОДИН РАЗ**
2. Инцидент переходит "Решен" → "Закрыто" → **БЕЗ повторного сигнала**
3. Инцидент переходит "Решен" → "Отклонен" → **БЕЗ повторного сигнала**

**Важно:** Для алертов при переходе "Руководитель" → "Закрыто" также сработает зеленый сигнал ОДИН РАЗ.

**Зеленый светофор мигает 30 секунд и гаснет.**

---

### 🟡 ЖЕЛТЫЙ светофор (алерты, тип 13802)

**Показывается ОДИН РАЗ** когда алерт становится активным:

**Поведение:**
1. Алерт создается → 🟡 YELLOW_BLINK (120 сек) - **ОДИН РАЗ**
2. Алерт переходит между активными статусами → **БЕЗ повторного сигнала**
3. Алерт переходит в решенный статус → 🟢 GREEN_BLINK (30 сек) - **ОДИН РАЗ**

**Желтый светофор горит 120 секунд и гаснет.**

---

## Примеры сценариев

### Сценарий 1: Жизненный цикл инцидента

```
1. Инцидент создан (статус: Создан)
   → 🔴 RED_BLINK 120 сек - ПОКАЗЫВАЕТСЯ

2. Инцидент назначен (статус: Создан → Назначен)
   → Без сигнала, только логирование

3. Инцидент взят в работу (статус: Назначен → Исполнитель)
   → Без сигнала, только логирование

4. Инцидент передан руководителю (статус: Исполнитель → Руководитель)
   → Без сигнала, только логирование

5. Инцидент решен (статус: Руководитель → Решен)
   → 🟢 GREEN_BLINK 30 сек - ПОКАЗЫВАЕТСЯ

6. Инцидент закрыт (статус: Решен → Закрыто)
   → Без сигнала, только логирование
```

**Итого:** 2 сигнала за весь жизненный цикл инцидента:
- 1x 🔴 RED при создании
- 1x 🟢 GREEN при решении

---

### Сценарий 2: Восстановление алерта

```
1. Алерт создан (статус: Создан)
   → 🟡 YELLOW_BLINK 120 сек - ПОКАЗЫВАЕТСЯ

2. Алерт передан руководителю (статус: Создан → Руководитель)
   → Без сигнала, только логирование

3. Алерт восстановился (статус: Руководитель → Закрыто)
   → 🟢 GREEN_BLINK 30 сек - ПОКАЗЫВАЕТСЯ
```

**Итого:** 2 сигнала за весь жизненный цикл алерта:
- 1x 🟡 YELLOW при создании
- 1x 🟢 GREEN при закрытии

---

### Сценарий 3: Отклонение инцидента

```
1. Инцидент создан (статус: Создан)
   → 🔴 RED_BLINK 120 сек - ПОКАЗЫВАЕТСЯ

2. Инцидент отклонен (статус: Создан → Отклонен)
   → 🟢 GREEN_BLINK 30 сек - ПОКАЗЫВАЕТСЯ
```

**Итого:** 2 сигнала:
- 1x 🔴 RED при создании
- 1x 🟢 GREEN при отклонении

---

## Технические детали

### JQL запрос (из server.properties):

```jql
project = ITSMJIRA AND issuetype IN (11206, 13802) AND status NOT IN (Closed,Resolved,Done)
```

**Важно:** JQL уже фильтрует закрытые статусы, НО система все равно обрабатывает переход в эти статусы для отправки GREEN_BLINK.

### Отслеживание состояний:

Система использует 2 коллекции:

1. **`activeIncidents`** - Map<String, String>
   - Хранит активные задачи и их текущие статусы
   - Используется для определения переходов

2. **`processedIssues`** - Set<String>
   - Хранит все обработанные задачи
   - Предотвращает повторную обработку

### Логика обработки:

```
IF задача в activeIncidents:
    IF статус изменился на решенный:
        → GREEN_BLINK (ОДИН РАЗ)
        → Удалить из activeIncidents
    ELSE IF статус остается активным:
        → Только логирование, БЕЗ сигнала
ELSE IF задача НЕ в processedIssues:
    IF статус активный:
        → RED_BLINK или YELLOW_BLINK (ОДИН РАЗ)
        → Добавить в activeIncidents
```

---

## Настройки

### Длительность сигналов:

| Сигнал | Цвет | Длительность |
|--------|------|--------------|
| RED_BLINK | Красный | 120 секунд |
| YELLOW_BLINK | Желтый | 120 секунд |
| GREEN_BLINK | Зеленый | **30 секунд** |
| QUEUE_RED | Красный | До снятия админом |
| QUEUE_GREEN | Зеленый | До снятия админом |

### Интервал опроса JIRA:

`jira.poll.interval=5` - опрос каждые 5 минут

### Адрес сервера (client.properties):

`server.address=ws://10.10.90.170:52521`

---

## Консольные логи

### При создании инцидента:

```
🔍 Опрос JIRA на наличие новых инцидентов/алертов...
   Найдено записей: 1
📋 Новая активная задача: ITSMJIRA-123456 (тип: Incident, ID: 11206)
   Инцидент: Сбой сервера БД | Приоритет: высокий | Статус: Создан | Автор: Иван
   🔴 Отправка сигнала: RED_BLINK (инцидент) - ОДИН РАЗ
```

### При переходе статуса:

```
🔍 Опрос JIRA на наличие новых инцидентов/алертов...
   Найдено записей: 1
🔄 Обновление статуса: ITSMJIRA-123456
   Статус: Создан → Назначен (без повторного сигнала)
```

### При решении:

```
🔍 Опрос JIRA на наличие новых инцидентов/алертов...
   Найдено записей: 1
✅ Задача решена: ITSMJIRA-123456 (тип: Incident)
   Статус: Руководитель → Решен
   🟢 Отправка сигнала: GREEN_BLINK (решение)
```

---

## Иконка приложения

Добавлена иконка `44_85245.ico` для обоих окон:
- Окно выбора роли
- Окно светофора

---

## Что изменилось от предыдущей версии:

1. ❌ **Удалено:** Сигналы при каждом обновлении статуса
2. ✅ **Добавлено:** Показ сигналов **ОДИН РАЗ**
3. ✅ **Добавлено:** Различение активных/решенных статусов
4. ✅ **Изменено:** Зеленый светофор - 30 секунд (было 120)
5. ✅ **Исправлено:** server.address = ws://10.10.90.170:52521
6. ✅ **Добавлено:** Иконка приложения

---

## Файлы

- `target/svetoofor-server.jar` - серверное приложение
- `target/svetoofor-client.jar` - клиентское приложение
- `target/svetoofor-installer.jar` - установщик для Windows

Все готово к развертыванию! 🎉
